# Orderly Signature Generation

- Create a .env file and add relevant data using .env.sample
- Run this command :

```sh
npm i
```

- Check the file **4-orderly-sign.js** update the request method and request url as per the need
- Run this command :

```sh
node 4-orderly-sign.js
```
